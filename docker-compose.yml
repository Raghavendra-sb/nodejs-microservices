version: "3.9"

services:

  mongodb:
    image: mongo:5
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  user-service:
    build: ./userService
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

volumes:
  mongo_data: